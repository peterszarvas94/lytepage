import { PluginOption } from "vite";
export declare const watch: (config: {
    pattern: string | string[];
    command: string | string[];
    silent?: boolean;
    timeout?: number;
    onInit?: boolean;
}) => PluginOption;
